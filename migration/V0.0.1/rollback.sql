-- =============================================================================
-- DevHub: rollback initial schema
-- Purpose: drop objects created by the initial bootstrap migration
-- Author:  Raven390
-- Notes:
--   * Удаляет ВСЕ объекты схемы devhub (CASCADE)
--   * Схему keycloak пробуем удалить только если пустая (RESTRICT)
--   * Расширение pg_trgm по умолчанию НЕ удаляем
-- =============================================================================

BEGIN;

-- ---------------------------------------------------------------------------
-- Полный откат схемы DevHub (таблицы, индексы, констрейнты, последовательности)
-- ---------------------------------------------------------------------------
DROP SCHEMA IF EXISTS devhub CASCADE;


COMMIT;
